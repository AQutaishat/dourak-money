<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- firebase_analytics's own manifest requests these unconditionally, even though
         google_analytics_adid_collection_enabled=false (below) stops it from actually
         being used. Play Console flags the permission's mere presence regardless of that
         setting, so remove it at merge time to keep the "No" advertising-ID declaration
         truthful and avoid the Play Console warning. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_AD_ID" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_ATTRIBUTION" tools:node="remove" />

    <application
        android:label="Dourak"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Android App Link for the WhatsApp invite link sent by an organizer:
                 https://dourak.money/invite/{token}. autoVerify requires
                 https://dourak.money/.well-known/assetlinks.json to list this app's
                 package + signing-cert fingerprint (see docs/mobile-gap.md). Until that
                 file is live, Android still offers this app in the app chooser for the
                 link; the web page handles it for anyone without the app either way. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="dourak.money" android:pathPrefix="/invite/" />
            </intent-filter>

            <!-- Fallback custom scheme: dourak://invite/{token}. Never needs domain
                 verification, so an invite link always has a working in-app path. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="dourak" android:host="invite" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Dourak has no ads/attribution use for the Advertising ID — explicitly opt
             Firebase Analytics out of collecting it. This keeps the true answer to Play
             Console's "Does your app use advertising ID?" declaration as No, so the
             com.google.android.gms.permission.AD_ID permission is never required. -->
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />
    </application>

    <!-- url_launcher (WhatsApp share intents, §6.22 / mobile-plan §2) needs these
         queries declared under Android 11+ package visibility rules. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <package android:name="com.whatsapp" />
    </queries>
</manifest>
