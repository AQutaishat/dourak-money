# Web ↔ Mobile Feature Gap

Tracks web app changes that have **not yet been ported to the Flutter mobile app**. Whenever a change is made to `frontend/` and the equivalent isn't also made in `mobile/`, add an entry here. Remove an entry once the mobile side is implemented.

| Date | Web change | Where (web) | Mobile status |
|---|---|---|---|
| 2026-09-17 | Contributions are only marked "Late" after a 7-day grace period past the due date (previously late the instant the due date passed). Added `Contribution.GracePeriodDays = 7` in the domain layer (backend, so mobile gets the correct `status` value from the API automatically), plus an always-visible caption above the payments table explaining the rule: "Marked Late 7 days after the due date if still not fully paid." | `backend/src/Dourak.Domain/Entities/Contribution.cs`; `frontend/src/pages/CircleOverview/CurrentCycleTab.tsx` (caption); `frontend/src/i18n/en.json` / `ar.json` (`circle.gracePeriodHint`) | Backend logic applies automatically (same API). **Missing**: the explanatory caption in `mobile/lib/screens/circle_overview/current_cycle_tab.dart` and the `gracePeriodHint` string in `mobile/lib/l10n/strings.dart`. |

| 2026-09-18 | Circle details page header and Basic Info box unified into one shared code path for draft and active circles (previously two separate `isDraft ? ... : ...` branches / a `dense` prop toggling two different field sets) — now a single render, driven only by status data. Header: name + status badge (color mirrors the home-page card) + view-only badge + (if organizer and Active/Paused) an icon-only pause/resume/cancel menu trigger (text label "الإجراءات" removed, three dots only) all share one row; below it, one line with the unlabeled "{organizer} : {name} . {members} {count}" text (mirrors the home-page circle card) on one side and the creation date alone on the other (lands on the left in Arabic purely from the space-between row order, no forced styling), with a tooltip ("تاريخ الإنشاء"/"Created") on hover over the date. The top Close button was removed entirely (bottom Close row unaffected). Basic Info box: same fields/order for both draft and active — description → [start date / last payment month] → [payout amount / total amount] → [organizer / member count] — no circle-name or creation-date rows (both live in the header); the Edit button is the only thing that differs, shown only while Draft. | `frontend/src/pages/CircleOverview/BasicInfoBlock.tsx`; `frontend/src/pages/CircleOverview/CircleOverviewPage.tsx` | Not ported. Mobile's equivalents (`mobile/lib/screens/circle_overview/basic_info_tab.dart`, `circle_overview_screen.dart`) still show the old two-branch header, the old dense/non-dense box split, and the text+icon actions button. |
| 2026-09-18 | Current Cycle tab reworked further: (1) "مشاركة عبر واتساب" and the grace-period hint text both moved from above the payments table to below it (share button right-aligned via `justifyContent="flex-end"`, which lands left in Arabic); (2) the 4th stat card now shows the cycle's month ordinal ("الشهر" / "الأول", "الثاني", ...) instead of the next recipient's name; (3) the "إشعارات الدفع" heading and the organizer's claim-inbox button were removed — instead, a clickable colored badge ("دفعة معلقة" orange / "دفعة مقبولة" green) appears next to a member's row, right after the Status column, opening that one claim's detail dialog (approve/reject while Pending, read-only with evidence once Approved/Rejected); (4) the bottom action row is now shared between draft and active circles in one code path — Close always on the right, and the other slot holds Activate (Draft) or the new `ConfirmPayoutButton` (Active, "تأكيد استلام صاحب الدور"), always on the left in both languages via a fixed `direction: "ltr"` + `order` lock (no more per-language swap). | `frontend/src/pages/CircleOverview/CurrentCycleTab.tsx`; `frontend/src/pages/CircleOverview/PaymentClaimDialogs.tsx` (`ClaimDetailDialog` replaces `ReviewPaymentClaimsDialog`); `frontend/src/pages/CircleOverview/StatusChip.tsx` (`PaymentClaimBadge`); `frontend/src/pages/CircleOverview/ConfirmPayoutButton.tsx` (new); `frontend/src/pages/CircleOverview/CircleOverviewPage.tsx` | Not ported. Mobile's `current_cycle_tab.dart` still has the old layout (share/grace-hint above the table, next-recipient card, the claims-inbox button) and `circle_overview_screen.dart` still has separate draft/active bottom bars with no Confirm-payout button there. |
| 2026-09-18 | Members list: name shown as "Name (email)" (or just email if no name) instead of name + phone/email on a separate line; phone no longer shown at all. The "Pending"/invitation-status chip is now hidden once a member has Accepted (no badge shown for the normal case). Draft circle's bottom action row: removed the white sticky panel around Activate, removed the arrow icon inside it, and merged Close + Activate onto one row — Arabic: Close left / Activate right; English: Close right / Activate left. | `frontend/src/pages/CircleOverview/MembersTab.tsx`; `frontend/src/pages/CircleOverview/ActivateCircleButton.tsx`; `frontend/src/pages/CircleOverview/CircleOverviewPage.tsx` | Ported in the same change: `mobile/lib/screens/circle_overview/members_tab.dart`, `mobile/lib/screens/circle_overview/activate_circle_button.dart`, `mobile/lib/screens/circle_overview/circle_overview_screen.dart`. |
| 2026-09-18 | Organizer can now edit the contribution amount from the circle's basic-info edit form while still a Draft (previously locked, only name/description/start date were editable). `SavingsCircle.UpdateBasicInfo` now takes `contributionAmount` and `UpdateCircleBasicInfoCommand`/the `PUT /circles/{id}/basic-info` request now require it. | `backend/src/Dourak.Domain/Entities/SavingsCircle.cs`; `backend/src/Dourak.Application/Circles/Commands/CircleCommands.cs`; `backend/src/Dourak.Api/Controllers/CirclesController.cs`; `frontend/src/pages/CircleOverview/BasicInfoBlock.tsx`; `frontend/src/api/circles.ts` | Mobile updated in the same change (this was a breaking API contract change, not just a new feature — mobile's existing edit-basic-info call would otherwise fail validation): `mobile/lib/api/circles_api.dart`, `mobile/lib/screens/circle_overview/basic_info_tab.dart`. |
| 2026-09-17 | `SavingsCircle.Activate()` no longer requires the payout order to be saved beforehand — it now only requires at least 2 participating members, and defaults to the members' current order automatically if none was set/saved. | `backend/src/Dourak.Domain/Entities/SavingsCircle.cs` | Backend-only change, applies automatically via the same API — no mobile UI blocked this before, so no mobile change needed. |

| 2026-09-18 | Backend: a member can now edit (amount/note/evidence) or withdraw ("unsend") their own payment claim while it's still Pending — new `UpdatePaymentClaimCommand`/`WithdrawPaymentClaimCommand`, `PUT /api/payment-claims/{id}` and `DELETE /api/payment-claims/{id}`. Frontend: header's organizer/date line and the "المشاركة عبر الواتس"+grace-hint line both switched from relying on natural RTL bidi ordering to an explicit `direction: "ltr"` + `order` lock (the bidi-only approach didn't reliably put them on the left in Arabic). Current Cycle tab: month-ordinal card is now first (was last); a "دفعات الأعضاء" heading sits above the payments table; the claim badge for both the organizer and a member's own row now lives inside the Status column (last badge there) instead of the actions column; "Record payment" is hidden once a member is fully Paid; a member's own still-Pending claim chip is now clickable, opening an editable dialog (`MyClaimDialog`) with amount/note/evidence fields and a withdraw action, instead of the old plain non-clickable chip shown above the table. Circle header: `circle.organizer` label changed from "منظّم الجمعية" to "المنظم". Circle-details top bar: the standalone Delete button was removed — Delete now lives inside the same three-dot menu shown to the organizer on every status (pause/resume/cancel while Active/Paused, Delete otherwise). | `backend/src/Dourak.Domain/Entities/PaymentClaim.cs`; `backend/src/Dourak.Application/Circles/Commands/PaymentClaimCommands.cs`; `backend/src/Dourak.Api/Controllers/InvitationsController.cs`; `frontend/src/api/circles.ts`; `frontend/src/pages/CircleOverview/PaymentClaimDialogs.tsx` (`MyClaimDialog`); `frontend/src/pages/CircleOverview/CurrentCycleTab.tsx`; `frontend/src/pages/CircleOverview/CircleOverviewPage.tsx`; `frontend/src/pages/CircleOverview/StatusChip.tsx`; `frontend/src/i18n/en.json` / `ar.json` | Not ported. Mobile has no edit/withdraw UI for a submitted claim, no `دفعات الأعضاء` heading, the old next-recipient/last-position stat card, the old organizer wording, and no unified three-dot menu (still a separate Delete button for drafts). |

| 2026-09-18 | Fixed a real RTL bug: the `direction: "ltr"` lock used in several places (via MUI `sx`) was silently being rewritten back to `direction: "rtl"` in Arabic by the RTL emotion cache (`stylis-plugin-rtl` → `cssjanus`, which unconditionally flips any `direction: ltr` string it finds in generated CSS) — so those left/right locks never actually worked in Arabic despite looking correct in the source. Fixed by moving the lock into a native `style={{ direction: "ltr" }}` prop instead of `sx`, which is applied as a plain inline style and never passes through the emotion/stylis pipeline. Applied to: the circle header's organizer/date line (date now genuinely lands on the left in Arabic, right in English; organizer/member-count text stays on the right in Arabic), the bottom Close/Activate-or-Confirm row, and the WhatsApp-share/grace-period-hint line (now correctly dynamic per language: share button right + hint left in Arabic, mirrored in English — previously it silently rendered the same physical layout in both languages). Also: the "دفعات الأعضاء" heading above the payments table was made larger (`h6` instead of `subtitle1`) with more top margin. | `frontend/src/pages/CircleOverview/CircleOverviewPage.tsx`; `frontend/src/pages/CircleOverview/CurrentCycleTab.tsx` | Not ported — mobile has its own layout for these lines and never had this bug (no RTL emotion-cache pipeline there), so no action needed unless mobile's Arabic alignment is separately found to be wrong. |

| 2026-09-18 | Fixed three bugs in a member's own payment-claim UI (frontend-only, no backend change): (1) the RTL icon-placement regression this same day's `direction: "ltr"` positional locks introduced — forcing `direction: ltr` on a row also forces it (via CSS inheritance) onto any `Button` inside it, flipping `startIcon` to the wrong side; fixed by re-locking each such button's own `direction` back to the actual language (`style={{ direction: rtl ? "rtl" : "ltr" }}`) on the Close button and the WhatsApp-share button. (2) `MyClaimDialog`'s field-reset `useEffect` was keyed on the `claim` object itself, which react-query replaces with a new object reference on every background refetch — including the refetch React Query fires on window focus, which happens right when the native file picker closes — silently wiping a just-picked evidence file (or in-progress edits) before the member could hit Save; fixed by keying the reset on `open`/`claim?.id` instead, so it only resets when the dialog opens or the claim being edited actually changes. (3) the withdraw ("unsend") mutation had no `onError` handler, so a failed withdraw (e.g. the organizer had already reviewed the claim) looked like clicking the button did nothing; added the same error-surfacing pattern already used elsewhere. Also: a member's own Approved/Rejected claim chip (previously only the still-Pending one was clickable) now opens the same read-only detail dialog the organizer sees, including the rejection reason. | `frontend/src/pages/CircleOverview/CircleOverviewPage.tsx`; `frontend/src/pages/CircleOverview/CurrentCycleTab.tsx`; `frontend/src/pages/CircleOverview/PaymentClaimDialogs.tsx` (`MyClaimDialog`) | Not applicable — mobile has no equivalent editable claim dialog yet (see the 2026-09-18 entry above); these are web-only bug fixes to that same not-yet-ported flow. |

| 2026-09-18 | **Real bug fix (backend behavior change):** `Contribution.RecordPayment` used to *replace* `PaidAmount` with whatever the organizer typed, instead of adding it to what was already paid — recording a 30 top-up on top of an existing 50 payment set the total to 30 instead of 80. It also validated the new amount against the full expected amount rather than what's actually still outstanding, so overpaying past what's left silently did nothing client-side with no error. Fixed: `RecordPayment` now does `PaidAmount += amount` and validates `amount` against `ExpectedAmount - PaidAmount` (the remaining balance), throwing a `DomainException` if exceeded. Frontend: the "Record payment" dialog now defaults to and caps at the member's remaining outstanding balance (not the full contribution amount), and shows a red-bordered field with an inline error message both for a client-side over-limit and for a server rejection — previously the field just silently refused the value with no explanation. Also fixed: the Basic Info box's Edit button now uses `justifyContent: "flex-start"` (was `"flex-end"`) so it naturally lands on the right in Arabic and the left in English, instead of the reverse; and the header's organizer/date line's wrapping `Box` is now `width: "100%"` so the creation date locks to the actual left edge of the page instead of just the right edge of the (previously shrink-wrapped) name-row's own width. | `backend/src/Dourak.Domain/Entities/Contribution.cs`; `frontend/src/pages/CircleOverview/CurrentCycleTab.tsx`; `frontend/src/pages/CircleOverview/BasicInfoBlock.tsx`; `frontend/src/pages/CircleOverview/CircleOverviewPage.tsx`; `frontend/src/i18n/en.json` / `ar.json` | **Not ported — this is a real bug, and mobile's record-payment flow has the exact same bug** (mobile calls the same backend endpoint, so once mobile is rebuilt against this fixed backend the addition-not-replacement behavior applies automatically; but mobile's own payment dialog likely still defaults/caps to the full amount rather than the outstanding balance and has no equivalent error UI — needs its own UI fix in `mobile/lib/screens/circle_overview/current_cycle_tab.dart` or equivalent). |

| 2026-09-18 | Circle header (under the name) now also shows a third segment on the organizer/member-count line — "{headerAmounts} : {collected} / {expected} {currency}" — mirroring the collected/expected format already used elsewhere (`CurrentCycleSummaryCard`); only rendered once a cycle's dashboard is available (i.e. not for a Draft circle with no cycle yet). Also fixed a real UI bug in the "Record payment" dialog: closing it (either after a successful save or via Cancel) briefly showed the "cannot exceed the remaining outstanding amount" error message during MUI's ~200ms close-fade — caused by the outstanding-amount limit falling back to 0 once the dialog's target member state was cleared, which made the still-nonzero typed amount look like it exceeded the limit for that instant. Fixed by only evaluating that error condition while the dialog's target member is still set. | `frontend/src/pages/CircleOverview/CircleOverviewPage.tsx`; `frontend/src/pages/CircleOverview/CurrentCycleTab.tsx`; `frontend/src/i18n/en.json` / `ar.json` (`circle.headerAmounts`) | Not ported. Mobile's circle header has no equivalent collected/expected summary line, and its record-payment flow (if it has a similar dialog) should be checked for the same close-transition error-flash class of bug. |

| 2026-09-18 | Two frontend-only additions, no backend/API change: (1) the circle header now shows two extra status badges next to the circle status badge, visible only while Active: a collection badge ("تحت التحصيل"/"تم التحصيل") based on `dashboard.collected` vs `dashboard.expected`, and — only once collection is done — a payout badge ("بانتظار الدفع لصاحب الدور"/"تم الدفع لصاحب الدور") based on `dashboard.payoutStatus`. (2) Create Circle and the Basic Info edit form now collect the start date as a month/year picker (`type="month"`) plus a separate "يوم التحصيل" (collection day, 1–31) number field, combined client-side into the same full `startDate` the backend already expects (`buildDateFromMonthAndDay` in `frontend/src/utils/date.ts`, which clamps the day to the target month's actual length) — no backend schema change, since the day-of-month was always encoded in `startDate` and cycle due dates already carry it forward via `AddMonths`. The Basic Info display box now shows the start date as month+year only, with a new row directly beneath it showing the collection day (derived as `new Date(startDate).getDate()`) in the left column and nothing in the right column. | `frontend/src/pages/CircleOverview/CircleOverviewPage.tsx`; `frontend/src/pages/CreateCircle/CreateCirclePage.tsx`; `frontend/src/pages/CircleOverview/BasicInfoBlock.tsx`; `frontend/src/utils/date.ts` (new); `frontend/src/i18n/en.json` / `ar.json` | Not ported. Mobile's create-circle and edit-basic-info screens still use a single full-date picker, and its circle header has no equivalent collection/payout status badges. |

| 2026-09-18 | Two small frontend-only tweaks: (1) the collection/payout status badges added earlier today were moved from next to the circle name in the page header to next to the "صاحب الدور : {name}" recipient line at the top of the Current Cycle tab (same conditions/logic, just relocated). (2) The "الشهر" stat card now shows the month name in parentheses after the ordinal, e.g. "الأول (أكتوبر)" / "First (October)", derived from the cycle's due date. | `frontend/src/pages/CircleOverview/CircleOverviewPage.tsx`; `frontend/src/pages/CircleOverview/CurrentCycleTab.tsx` | Not ported — same as the two features these tweak (2026-09-18 entries above), mobile doesn't have the badges or the month-ordinal card yet. |

| 2026-09-18 | Three changes: (1) the collection/payout status badges (added earlier today, then moved once already) now sit on their own line under "صاحب الدور : {name}" instead of beside it. (2) The header's "{headerAmounts} : {collected} / {expected}" text dropped the trailing currency code — in English it was running into the next line/wrapping awkwardly. (3) **New feature, real backend change:** a month-by-month progress timeline — one dot per cycle — now renders on its own line under the circle name/organizer/amounts text (for any non-Draft circle). Each dot is a tooltip-only status marker (no numbers on the line itself): solid green = collected and paid out, solid blue = collected but payout still pending, solid red = due date passed without full collection, a larger highlighted dot = the current month, and a small hollow outline = an upcoming month. Backend: `ScheduleCycleDto`/`GetScheduleQuery` now also returns `CollectedAmount` (`c.Contributions.Sum(co => co.PaidAmount)`) per cycle, since the existing schedule endpoint only had the cycle's Pending/Completed status and payout status, not whether that cycle's collection was actually complete. | `backend/src/Dourak.Application/Circles/Dtos/CircleDtos.cs`; `backend/src/Dourak.Application/Circles/Queries/CircleQueries.cs`; `frontend/src/pages/CircleOverview/CircleTimeline.tsx` (new); `frontend/src/pages/CircleOverview/CircleOverviewPage.tsx`; `frontend/src/pages/CircleOverview/CurrentCycleTab.tsx`; `frontend/src/api/types.ts`; `frontend/src/i18n/en.json` / `ar.json` | Not ported. Mobile's circle header has no equivalent timeline, badges, or amounts summary. Note the backend DTO change (`ScheduleCycleDto` gained a field) is additive/non-breaking, so mobile's existing schedule call keeps working unchanged even without adopting the new field. |

| 2026-09-18 | Redesigned the month-by-month timeline added earlier today (frontend-only, no backend change this time): the connecting line is now a single continuous 6px bar (was 2px) spanning behind all the dots, each dot is labeled underneath with its month, "{collected} / {expected}" amount, and its status spelled out (e.g. "تم التحصيل، بانتظار الدفع لصاحب الدور") in the status's own color — tooltips are still there too, just no longer the only way to see the detail. The current month's dot is now the same size as every other dot (previously drawn larger) and instead gets a larger hollow ring around it to stand out. Added more top margin between the timeline and the organizer/amounts line above it. | `frontend/src/pages/CircleOverview/CircleTimeline.tsx`; `frontend/src/pages/CircleOverview/CircleOverviewPage.tsx` | Not ported — same as the original timeline feature (2026-09-18 entry above), mobile doesn't have it yet. |

| 2026-09-18 | Further timeline redesign (frontend-only): dot color now always reflects that month's actual state — orange for not (yet) fully collected, blue for collected-but-not-paid-out, green for collected-and-paid-out — applied uniformly to past, current and future months alike (no more separate "future = hollow/gray" treatment; every dot is solid). The connecting line is now two-toned: green for the segment(s) before/up-to the current month, gray beyond it. The current month's ring is now a fixed color (black) rather than state-colored, and is deliberately offset lower than the line instead of centered on it, so it reads as a "you are here" marker sitting on the line rather than just another node. Label under each dot is now "{month name, no year} {collected}/{expected}" on one line and the status spelled out on the next (three states only: collected-and-paid, collected-not-paid, not-collected — the separate "current month"/"upcoming month" labels were dropped since the dot color+ring now carry that). Also hid the small scrollbar that showed up on the timeline's horizontal-scroll container in Arabic (`scrollbarWidth: none` + the `::-webkit-scrollbar` equivalent) — it's still scrollable, just not visibly showing a scrollbar track. | `frontend/src/pages/CircleOverview/CircleTimeline.tsx`; `frontend/src/i18n/en.json` / `ar.json` | Not ported — same as the original timeline feature (2026-09-18 entries above). |

| 2026-09-18 | Timeline polish (frontend-only): rewrote the dot/ring/line vertical positioning to use explicit absolute offsets from one fixed reference point (`LINE_Y`) instead of margin math derived from the dot area's total height, so every non-current dot's center is guaranteed to sit exactly on the line regardless of how far the current month's ring is dropped below it. The current month's ring color changed from black to gray. The collected/expected amount label lost the spaces around the slash ("50/100" instead of "50 / 100"). | `frontend/src/pages/CircleOverview/CircleTimeline.tsx` | Not ported — same as the original timeline feature (2026-09-18 entries above). |

| 2026-09-18 | Timeline status/label tweaks (frontend-only): the not-fully-collected state's label changed from "لم يتم التحصيل" to "تحت التحصيل" (was implying collection failed rather than just still in progress). Any month after the current one is now always drawn gray with a new "لم تبدأ بعد" (not started yet) status, regardless of its (always-zero, meaningless) collected amount — previously a future month could confusingly show the "under collection" orange color since its data looks identical to a genuinely-behind past month. The amount label order under each dot was swapped from "collected/expected" to "expected/collected" (e.g. "100/50" instead of "50/100"). | `frontend/src/pages/CircleOverview/CircleTimeline.tsx`; `frontend/src/i18n/en.json` / `ar.json` | Not ported — same as the original timeline feature (2026-09-18 entries above). |

| 2026-09-18 | Reverted the earlier "current month drops below the line" timeline treatment — the line now passes through the exact center of every dot, including the current month's ring, rather than the ring sitting offset below it. | `frontend/src/pages/CircleOverview/CircleTimeline.tsx` | Not ported — same as the original timeline feature (2026-09-18 entries above). |

| 2026-09-18 | **New feature, real backend/schema change:** the Schedule tab was completely redesigned from a flat one-row-per-cycle summary table into a full per-month breakdown. Each month gets its own large heading, with a collection-status icon and a payout-status icon next to it once that month is in the past (green check / hourglass for collection, paid icon / hourglass for payout), the collected/total amount underneath, and a full member table (name, email as its own column, amount(s) paid, date(s) paid, and a clickable claim badge when that installment came from a member's self-reported claim — opens the same read-only/approve-reject `ClaimDetailDialog` used elsewhere). A month after the current one simply has no payment data yet, so those cells show "—". **Schema change:** payments were previously only a single running `PaidAmount`/`PaidAt` on `Contribution`, with no record of individual installments — so there was no way to show "member paid 30 then later paid 20" as two separate rows. Added a new `ContributionPayment` child entity (one row per `RecordPayment` call, i.e. per organizer-recorded payment or per approved claim) and a migration (`AddContributionPayments`); `Contribution.PaidAmount` keeps working exactly as before (still the running total), the new table is purely additive history alongside it. New read endpoint `GET /circles/{id}/months-detail` (`GetCircleMonthsDetailQuery`) returns every cycle with its full per-member payment breakdown for this. | `backend/src/Dourak.Domain/Entities/ContributionPayment.cs` (new); `backend/src/Dourak.Domain/Entities/Contribution.cs`; `backend/src/Dourak.Application/Common/Interfaces/IAppDbContext.cs`; `backend/src/Dourak.Infrastructure/Persistence/DourakDbContext.cs`; `backend/src/Dourak.Infrastructure/Persistence/Migrations/20260918202050_AddContributionPayments.cs` (new); `backend/src/Dourak.Application/Circles/Dtos/CircleDtos.cs`; `backend/src/Dourak.Application/Circles/Queries/CircleQueries.cs`; `backend/src/Dourak.Api/Controllers/CirclesController.cs`; `frontend/src/pages/CircleOverview/ScheduleTab.tsx`; `frontend/src/pages/CircleOverview/CircleOverviewPage.tsx`; `frontend/src/api/circles.ts`; `frontend/src/api/types.ts`; `frontend/src/i18n/en.json` / `ar.json` | **Backend schema change — needs mobile attention.** The new `ContributionPayment` table/migration and the additive `GET /months-detail` endpoint don't break mobile's existing calls (nothing was removed or changed shape), but mobile's own Schedule/history screen has none of the new per-month breakdown UI and won't surface individual installment history even though the data now exists server-side. |

| 2026-09-18 | Schedule tab (now titled "تاريخ الدفعات"/"Payment History", was "جدول الجمعية"/"Schedule") polish, plus one real bug fix: (1) the plain check/hourglass/paid icons next to each past month were replaced with the exact same colored `Chip` badges used under "صاحب الدور" in the Current Cycle tab (`circle.collectionDone`/`collectionUnderway`, `payoutPaidBadge`/`payoutPendingBadge`), for visual consistency; the current month now also gets its own "الشهر الحالي" badge. (2) The standalone claim-status column was removed — the claim badge (دفعة مقبولة/مرفوضة) now sits inline right after that installment's amount in the Amount column instead. (3) Added a new "مجموع المبالغ المدفوعة" (total paid) column showing the contribution's running total alongside the per-installment breakdown. (4) Added the word "المبالغ" before the collected/total figure under each month's heading. (5) **Real bug, now fixed:** every contribution paid *before* today's `ContributionPayment` history feature existed has an empty `payments` array (the table only started being populated going forward), so all past/current months were showing no amount or date at all even though money had clearly been paid — fixed by having the backend also return the contribution's original single `PaidAt`, and the frontend falls back to a synthetic one-entry payment list (`{amount: paidAmount, paidAt}`) whenever the real history is empty but an amount was paid. | `backend/src/Dourak.Application/Circles/Dtos/CircleDtos.cs`; `backend/src/Dourak.Application/Circles/Queries/CircleQueries.cs`; `frontend/src/pages/CircleOverview/ScheduleTab.tsx`; `frontend/src/api/types.ts`; `frontend/src/i18n/en.json` / `ar.json` | Not ported — same as the Schedule tab redesign (2026-09-18 entry above). Note the "payments before this feature existed have no per-installment history, only the single running total" caveat this fix works around also applies to any mobile screen that later reads the new `payments` array directly. |

| 2026-09-19 | **Critical bug fix (backend, real money-affecting bug):** approving a payment claim on a contribution that already had a partial payment recorded threw a console/API error and could never succeed. Root cause: yesterday's "RecordPayment now adds instead of replaces" change wasn't propagated to `ReviewPaymentClaimCommandHandler`, which still computed the intended *new total* itself (`PaidAmount + ClaimedAmount`) and handed that total to `RecordPayment` — which then added it *again* on top of the existing balance, double-counting the amount and tripping the "cannot exceed outstanding" validation the moment anything had already been paid. Fixed by passing `claim.ClaimedAmount` directly and letting `RecordPayment` do the addition itself. Added a regression test (`ApprovingAClaim_OnAnAlreadyPartiallyPaidContribution_AddsOnTopInsteadOfDoubleCounting`) covering exactly this scenario (two manual payments, then an approved claim on top). | `backend/src/Dourak.Application/Circles/Commands/PaymentClaimCommands.cs`; `backend/tests/Dourak.Application.Tests/Phase2WorkflowTests.cs` | Mobile hits the same backend endpoint, so it was hitting the exact same failure and is now fixed automatically — no mobile-side change needed. |
| 2026-09-19 | **New feature (backend + frontend):** the Schedule tab (renamed "الدورات الشهرية"/"Monthly Cycles", previously "تاريخ الدفعات"/"Payment History") now shows a fully unified payment history per member per month, merging organizer-recorded payments with claims at every stage: a Pending or Rejected claim gets its own row with its status badge right next to the amount (but doesn't count toward the running total, since no payment was ever recorded for it); an Approved claim is merged into the single payment row it produced (not shown twice). This needed a new `ContributionPayment.PaymentClaimId` column (nullable FK, migration `AddPaymentClaimLinkToContributionPayments`) set whenever `RecordPayment` is called from an approved claim, so the query can tell "this installment came from claim X" apart from a plain organizer-recorded one. The claim-derived rows/badges are only visible to the organizer or the member's own row (same privacy rule as everywhere else) — masked to a plain unlabeled amount for anyone else. Also: dates in the table are now `dd/mm/yy` (numeric month, 2-digit year) instead of a spelled-out month; currency codes were removed from the table's amount cells (kept on the summary line above the table); added a "مجموع المبالغ المدفوعة"/total-paid column; and the collection/payout status badges next to each month heading now also show for the *current* month (previously only past months got them). | `backend/src/Dourak.Domain/Entities/ContributionPayment.cs`; `backend/src/Dourak.Domain/Entities/Contribution.cs`; `backend/src/Dourak.Application/Circles/Commands/PaymentClaimCommands.cs`; `backend/src/Dourak.Application/Circles/Dtos/CircleDtos.cs`; `backend/src/Dourak.Application/Circles/Queries/CircleQueries.cs`; `backend/src/Dourak.Infrastructure/Persistence/Migrations/20260918211930_AddPaymentClaimLinkToContributionPayments.cs` (new); `frontend/src/pages/CircleOverview/ScheduleTab.tsx`; `frontend/src/api/types.ts`; `frontend/src/i18n/en.json` / `ar.json` | Not ported — same as the original Schedule tab redesign (2026-09-18 entries above); the new claim/payment unification is an additional layer mobile doesn't have any of yet. |

| 2026-09-19 | Three frontend-only fixes: (1) approving/rejecting a claim from the Monthly Cycles tab left that claim's badge showing its stale pre-review status — the review mutation's cache invalidation never included `["monthsDetail", circleId]`, only `paymentClaims` and `dashboard`; added it. (2) Unified the claim-status badge: `PaymentClaimBadge` and `ClaimStatusChip` were two near-identical components with different wording for the same states (e.g. Approved showed "دفعة مقبولة" in the Current Cycle tab's organizer view but "تمت الموافقة" in Monthly Cycles) — merged into the single `ClaimStatusChip`, deleted `PaymentClaimBadge`, and every screen (Current Cycle tab, both organizer and member views, and Monthly Cycles) now renders the identical component with identical labels/colors. (3) Added the same collection-progress `LinearProgress` bar shown on this circle's card on the home page to the Current Cycle tab, under the recipient name and the collection/payout badges. | `frontend/src/pages/CircleOverview/StatusChip.tsx`; `frontend/src/pages/CircleOverview/CurrentCycleTab.tsx`; `frontend/src/pages/CircleOverview/PaymentClaimDialogs.tsx` | Not ported — mobile has its own claim-status widgets and no progress bar on its Current Cycle equivalent; this consistency pass and the progress bar are web-only so far. |

| 2026-09-19 | Circle-details layout restructuring (frontend-only, no backend change): (1) the "{headerAmounts} : {collected}/{expected}" text moved from the page header (under the circle name) to sit next to the collection-status badge under "صاحب الدور" in the Current Cycle tab. (2) The Basic Info box (description, start date, collection day, contribution amount, organizer, member count) is no longer tab-specific content — it now renders exactly once in `CircleOverviewPage`, above the tab bar and below the month timeline, shared identically by every circle status. Draft circles no longer have a separate "Basic Info" tab (`DRAFT_TABS` dropped that entry — draft tabs are now just Members/Payout Order), and `CurrentCycleTab` no longer renders its own copy. (3) The "تأكيد استلام صاحب الدور" button moved out of the page's shared bottom action row (next to Close) into the Current Cycle tab itself, so it's only visible while that tab is open instead of on every tab. | `frontend/src/pages/CircleOverview/CircleOverviewPage.tsx`; `frontend/src/pages/CircleOverview/CurrentCycleTab.tsx`; `frontend/src/pages/CircleOverview/BasicInfoBlock.tsx`; `frontend/src/pages/CircleOverview/ConfirmPayoutButton.tsx` | Not ported — mobile's circle-details screen still has Basic Info as its own draft tab, the amounts text in the header, and the confirm-payout button in its own bottom bar. |

| 2026-09-19 | Several small consistency/content changes: (1) the timeline dots now show "(recipient name)" in parentheses next to the month name (was the collected/expected amounts), and the collected/expected figure moved down to sit next to the status text — only shown when that month's status is "تحت التحصيل" (still under collection); once fully collected the badge alone is enough. (2) The Arabic "صاحب الدور" label under the recipient's name (Current Cycle tab) was shortened to just "دور". (3) The Monthly Cycles tab's per-month header now shows "دور: {recipientName}", then the collected-amounts line (relabeled "المبالغ المحصلة", only shown while under collection), then — once the organizer has confirmed the payout — a line reading "تم دفع مبلغ {amount} لصاحب الدور بتاريخ {date}" (backend: added `PayoutActualAmount`/`PayoutPaidAt` to `CircleMonthDto`, sourced from the existing single-installment `Payout.ActualAmount`/`PaidAt` — Phase 1 payouts aren't tracked as multiple installments the way contributions now are, so this is always at most one line). (4) **Real ordering bug fixed:** every member-facing table (Current Cycle, Monthly Cycles, Members) was listing members in raw database/join order instead of their circle payout-queue order — fixed by having `GetMembersQuery`, `GetCurrentCycleDashboardQuery`, and `GetCircleMonthsDetailQuery` all order by `PayoutPosition` (falling back to member id for anyone without a position yet, e.g. a still-Draft circle). | `backend/src/Dourak.Application/Circles/Dtos/CircleDtos.cs`; `backend/src/Dourak.Application/Circles/Queries/CircleQueries.cs`; `frontend/src/pages/CircleOverview/CircleTimeline.tsx`; `frontend/src/pages/CircleOverview/ScheduleTab.tsx`; `frontend/src/pages/CircleOverview/CircleOverviewPage.tsx`; `frontend/src/api/types.ts`; `frontend/src/i18n/en.json` / `ar.json` | The member-ordering fix applies automatically to mobile too (same backend queries, additive-only DTO changes — nothing removed). The timeline/Monthly-Cycles wording and layout changes are web-only, same as the features they build on (see earlier 2026-09-18/19 entries). |

| 2026-09-19 | Frontend-only polish, no backend change: (1) Timeline dots — the collected/expected figure shown next to the "تحت التحصيل" status is now gray (`text.secondary`) instead of the status's own color, dropped the "colon" separator, and is wrapped in an isolated `dir="ltr"`/`unicodeBidi: isolate` span so the number/slash sequence can't get visually bidi-reordered inside the surrounding Arabic text (the same class of bug hit a few times before in this app). (2) Monthly Cycles tab — while a month is still under collection, the collected/total line now comes first (right under the month name), then "دور: {name}", then the payout-paid line, instead of recipient-then-amounts; the payout line now names the actual recipient ("تم دفع مبلغ 500 لأنس بتاريخ ...") instead of the generic "لصاحب الدور" phrase; and all dates in this tab (the table and the payout line) switched from dd/mm/yy to yy/mm/dd. (3) Increased the gap between one month's table and the next month's heading. | `frontend/src/pages/CircleOverview/CircleTimeline.tsx`; `frontend/src/pages/CircleOverview/ScheduleTab.tsx`; `frontend/src/i18n/en.json` / `ar.json` | Not ported — same as the timeline/Monthly-Cycles features these tweak (see earlier 2026-09-18/19 entries). |

| 2026-09-20 | Two frontend-only changes: (1) Members tab: each row now shows the member's name and email as two side-by-side columns (email in its own `text.secondary` column next to the name) instead of "Name (email)" on one line or name/email stacked. (2) Create Circle: the organizer is now automatically added as a member of their own circle right after creation (`circlesApi.addSelfAsMember(id)` called immediately after `create`), instead of requiring them to use the separate "أضفني كعضو" button afterward. | `frontend/src/pages/CircleOverview/MembersTab.tsx`; `frontend/src/pages/CreateCircle/CreateCirclePage.tsx` | Not ported. Mobile's `members_tab.dart` still shows name+email combined/stacked, and its create-circle flow doesn't auto-add the organizer as a member. |

| 2026-09-20 | **New feature (backend + frontend):** the Add Member dialog no longer treats "invite someone not on Dourak yet" as a pure share-only action with no record — it now creates a real Pending `CircleMember` row (name-only, no `UserId` yet) carrying a one-time `InviteToken`, so that person shows up in the members table right away as "لم يقبل بعد" just like an invited registered user. The "دعوة شخص غير مسجل في دورك" section/description was removed; "دعوة عبر واتساب" is now a button right next to "إضافة", and clicking it first opens a small dialog asking for the person's name (used as their temporary display name until they respond) before sending the WhatsApp message — the message now links to `/invite/{token}` instead of the bare app URL. Opening that link stashes the token (`localStorage`, `frontend/src/utils/inviteToken.ts`) and sends the visitor to log in/register; once authenticated, the dashboard's `PendingInvitationsSection` links the stashed token to their account (`POST /api/invitations/link/{token}`) and the invitation then shows up and is accepted/declined through the exact same UI as any other invitation. Backend: `CircleMember.InviteToken` (new nullable column, migration `AddCircleMemberInviteToken`), `CircleMember.InviteUnregistered`/`LinkToUser` domain methods, `InviteUnregisteredMemberCommand` (`POST /circles/{id}/members/invite-unregistered`), `LinkInvitationTokenCommand` (`POST /invitations/link/{token}`). | `backend/src/Dourak.Domain/Entities/CircleMember.cs`; `backend/src/Dourak.Application/Circles/Commands/MembershipCommands.cs`; `backend/src/Dourak.Api/Controllers/CirclesController.cs`; `backend/src/Dourak.Api/Controllers/InvitationsController.cs`; `backend/src/Dourak.Infrastructure/Persistence/Migrations/20260920103116_AddCircleMemberInviteToken.cs` (new); `frontend/src/pages/CircleOverview/AddMemberDialog.tsx`; `frontend/src/pages/Invite/InvitePage.tsx` (new); `frontend/src/pages/Dashboard/PendingInvitationsSection.tsx`; `frontend/src/utils/inviteToken.ts` (new); `frontend/src/utils/whatsapp.ts`; `frontend/src/api/circles.ts`; `frontend/src/App.tsx`; `frontend/src/i18n/en.json` / `ar.json` | **Not ported — real feature gap.** Mobile's add-member flow still has the old "share-only, no record" WhatsApp invite (if it has one at all) and no `/invite/:token` deep-link handling; it needs its own token-linking screen/logic once it adds deep-link support, plus the same backend endpoints (already usable as-is, this is additive). |

| 2026-09-20 | Three changes: (1) Add Member dialog: "دعوة عبر واتساب" moved onto the same line as the "إضافة مستخدم مسجل" heading, aligned to the opposite end of that line (`justifyContent: space-between`) instead of sitting in its own row below; sending the WhatsApp invite (entering the name and confirming) now closes both the small name-prompt dialog and the whole Add Member dialog, instead of leaving them open. (2) **New backend behavior:** any member becomes participating with an immediate payout position — last in line — the instant they're added or their invitation is accepted, instead of only getting a position once the organizer manually saves an order in the Payout Order tab. New `SavingsCircle.AppendMemberToPayoutOrder` domain method, called from `AddMemberCommandHandler`, `AddSelfAsMemberCommandHandler`, `AddUserMemberCommandHandler`'s beta-auto-accept branch, and `RespondToInvitationCommandHandler`'s accept branch. (3) **New backend + frontend:** moving a member up/down in the Payout Order tab is now persisted immediately (new `SavingsCircle.MovePayoutPosition` domain method, `MovePayoutPositionCommand` / `POST /circles/{id}/payout-order/{memberId}/move`) instead of only updating local state until a "حفظ" button was pressed — that Save button was removed from the tab entirely. A pre-existing test (`Activate_WithoutAllMembersInOrder_Throws`) assumed adding members left the payout order empty until a manual save; rewritten as `AddingMembers_AutomaticallyRegistersThemInPayoutOrder_SoConfirmingNeverNeedsAManualSave` to assert the new (intended) behavior. | `backend/src/Dourak.Domain/Entities/SavingsCircle.cs`; `backend/src/Dourak.Application/Circles/Commands/CircleCommands.cs`; `backend/src/Dourak.Application/Circles/Commands/MembershipCommands.cs`; `backend/src/Dourak.Api/Controllers/CirclesController.cs`; `backend/tests/Dourak.Application.Tests/CircleWorkflowTests.cs`; `frontend/src/pages/CircleOverview/AddMemberDialog.tsx`; `frontend/src/pages/CircleOverview/PayoutOrderTab.tsx`; `frontend/src/api/circles.ts` | **Backend behavior change — needs mobile attention.** Mobile's add-member/accept-invitation flows hit the same endpoints, so members will start getting auto-assigned payout positions automatically (no mobile code change needed for that part). But mobile's own Payout Order screen (if it has drag/move controls) still relies on a manual "save the whole order" call — it should be pointed at the new `move` endpoint (or keep using `setManualOrder` with the full list, which still works) to match the "no Save button" web behavior, and its Add Member dialog's WhatsApp-invite layout/close-both-dialogs behavior isn't ported either. |

| 2026-09-20 | **Real bug fix (backend):** the newly-added "move payout position immediately" endpoint (`MovePayoutPositionCommand`, shipped earlier today) was throwing a 500 on every call — swapping two `PayoutPosition` rows' `Position` values in place made EF Core try to `UPDATE` both rows into each other's still-occupied value within one unique `(CircleId, Position)` index, which it can't order without violating the constraint ("circular dependency" save error). This is why the arrow buttons appeared to silently revert: the API call always failed, so the optimistic local reorder in `PayoutOrderTab` never actually got persisted, and the next background refetch snapped the list back to the old order. Fixed `SavingsCircle.MovePayoutPosition` to reuse the existing clear-and-recreate approach `SetManualPayoutOrder` already uses (delete-then-insert has no such ordering problem) instead of mutating the two rows' `Position` in place. Verified end-to-end against the running API (not just an in-memory handler test, which didn't exercise the real unique-index constraint). Also added `Activate_WithAPendingInvitee_Throws`/`MovePayoutPosition_PersistsImmediately_NoManualSaveNeeded` tests, and updated `InvitationTests.DeclinedAndPendingMembers_AreExcludedFromTheScheduleAndPayoutOrder` for the new activation rule below. | `backend/src/Dourak.Domain/Entities/SavingsCircle.cs`; `backend/tests/Dourak.Application.Tests/CircleWorkflowTests.cs`; `backend/tests/Dourak.Domain.Tests/InvitationTests.cs` | The bug was backend-only and shared by any client hitting the same endpoint — already fixed for both web and any future mobile caller. |
| 2026-09-20 | **New business rule (backend + frontend):** activating a circle now outright fails while any invited member is still Pending (hasn't accepted or declined) — previously a Pending invitee was just silently skipped from the order/schedule, letting the organizer activate around them with no chance for that person to still join. `SavingsCircle.Activate()` now throws before anything else if `Members.Any(m => m.IsActive && m.InvitationStatus == Pending)`. Frontend maps that specific error to a proper localized message ("الرجاء إزالة الأعضاء غير المؤكدين أولاً أو انتظار الرد منهم") in `ActivateCircleButton`, shown in the activation confirmation dialog exactly like the existing "at least two members" error. | `backend/src/Dourak.Domain/Entities/SavingsCircle.cs`; `frontend/src/pages/CircleOverview/ActivateCircleButton.tsx`; `frontend/src/i18n/en.json` / `ar.json` (`circle.activateBlockedPendingInvites`); `backend/tests/Dourak.Application.Tests/CircleWorkflowTests.cs`; `backend/tests/Dourak.Domain.Tests/InvitationTests.cs` | **Not ported.** Mobile hits the same backend endpoint so it will get the same 400 rejection automatically, but its own activate-circle UI (if any) needs the matching localized message instead of showing a raw/generic error. |
| 2026-09-20 | Two Members-tab-only frontend tweaks: (1) removed the "deactivate member" icon/action entirely (`PersonOffIcon`, `deactivateMutation`) — no replacement, this control is gone. (2) The "تاريخ الحركات" (view history) icon now only shows while the circle's status is Active — it no longer appears for a Draft/Paused/Completed/Cancelled circle. Also: the Add Member dialog's "دعوة عبر واتساب" button moved onto the same line as the "إضافة مستخدم مسجل" heading, pushed to the opposite end of that line, and submitting the WhatsApp-invite name now closes both the name-prompt dialog and the whole Add Member dialog (from the previous 2026-09-20 entry, corrected here after being reported as not fully wired). | `frontend/src/pages/CircleOverview/MembersTab.tsx`; `frontend/src/pages/CircleOverview/AddMemberDialog.tsx` | Not ported. Mobile's members screen still has whatever deactivate/history-icon behavior it shipped with. |

| 2026-09-20 | Two small frontend-only tweaks: (1) Members tab: the email column is now explicitly right-aligned in Arabic (`textAlign: rtl ? "right" : "left"`) — email strings render left-to-right internally regardless of page direction, so without this they visually drifted to the wrong side of their column in the Arabic layout. (2) Payout Order tab: "إعادة الترتيب" renamed to "إعادة الترتيب الاصلي" ("Reset to Original Order"), and it's now only shown once the organizer has actually changed the order — via the up/down arrows or a random draw — instead of always showing whenever any payout order existed (which, since positions are now auto-assigned on every member add, was effectively always). | `frontend/src/pages/CircleOverview/MembersTab.tsx`; `frontend/src/pages/CircleOverview/PayoutOrderTab.tsx`; `frontend/src/i18n/en.json` / `ar.json` (`circle.resetOrder`) | Not ported — cosmetic/UX-only, mobile's equivalent screens (if any) can be updated whenever mobile gets its own payout-order UI. |

| 2026-09-20 | Four frontend-only changes: (1) **Real bug fix** — the email column's right-alignment fix shipped earlier today used `sx={{ textAlign: ... }}`, which the RTL emotion cache silently mirrors (the same class of bug hit several times before in this app: any literal "left"/"right" written via `sx` gets flipped by `stylis-plugin-rtl`/`cssjanus`) — so it kept rendering left-aligned in Arabic. Fixed by moving it to a native `style` prop, which bypasses that pipeline. (2) Members tab: the payout-position "#N" chip moved out of the row's trailing actions into its own new column before the member's name (same Chip, just relocated). (3) **Real bug fix** — Payout Order tab's "إعادة الترتيب الاصلي" button called the old `resetOrder` endpoint, which wipes the order back to empty rather than restoring what it was before the arrows were used; fixed by snapshotting the order the moment before the first arrow-move or draw (`originalOrder` state) and having Reset call `setManualOrder` with that snapshot instead. (4) Basic Info box: added a collapse/expand chevron (`ExpandLess`/`ExpandMore`) in its own top-right corner, independent of the Edit button — clicking it hides/shows the field grid, for both Draft and Active circles since the box is shared. (5) Monthly Cycles tab: more breathing room between a month's heading and the lines under it (`mb: 2` on the heading row), and between those lines and the member table below them (`mt: 3` on the table). | `frontend/src/pages/CircleOverview/MembersTab.tsx`; `frontend/src/pages/CircleOverview/PayoutOrderTab.tsx`; `frontend/src/pages/CircleOverview/BasicInfoBlock.tsx`; `frontend/src/pages/CircleOverview/ScheduleTab.tsx` | Not ported — cosmetic/UX-only and mobile has none of these exact widgets (payout-position column, collapsible basic-info box) yet. |

| 2026-09-20 | Three frontend-only tweaks: (1) Monthly Cycles tab: the collected-amounts/recipient/payout-paid lines between the month heading and the member table are now shifted slightly left (`marginLeft: "-8px"` via native `style`, not `sx`, so the RTL emotion cache doesn't mirror it back to the right). (2) Same tab: the table column "مجموع المبالغ المدفوعة" renamed to "المجموع". (3) **Real bug fix:** the member-history page's "رجوع" (Back) button used a plain `ArrowBackIcon` (a left-pointing arrow) unconditionally, so in Arabic it visually pointed the wrong way — toward the end of the reading direction instead of back toward the start (right). Fixed by swapping to `ArrowForwardIcon` in Arabic, same as other directional-icon fixes earlier in this app (Close/WhatsApp-share buttons). | `frontend/src/pages/CircleOverview/ScheduleTab.tsx`; `frontend/src/i18n/ar.json`; `frontend/src/pages/CircleOverview/MemberHistoryPage.tsx` | Not ported. Mobile's Monthly-Cycles-equivalent screen doesn't have this exact layout, and mobile's own back button (if it uses a bare `Icons.arrow_back`) should be checked for the same RTL-mirroring bug. |

| 2026-09-20 | Monthly Cycles tab: fixed the left-shift of the collected-amounts/recipient/payout-paid lines added earlier today — it used a fixed `marginLeft: "-8px"` regardless of language, but that text is right-aligned in Arabic, so pushing its left edge does nothing visible; it needed `marginRight` instead, to actually push the block away from the right edge it's anchored to. Now applies `marginRight: "24px"` in Arabic and keeps the original `marginLeft: "-8px"` in English. | `frontend/src/pages/CircleOverview/ScheduleTab.tsx` | Not ported — same as the layout tweak it corrects (see earlier 2026-09-20 entry). |

| 2026-09-20 | **New feature (backend + frontend):** members' contributions can now be recorded ahead of schedule, for any month — not just the current cycle. (1) Monthly Cycles tab: every member row that hasn't fully paid that specific month (past, current, or a future month not due yet) now gets a "تسجيل دفعة" button in a new trailing column, identical to the Current Cycle tab's dialog — capped at that month's own remaining outstanding, can't exceed it, and only ever affects that one month's contribution. (No backend change was actually needed for this — `RecordContributionCommandHandler` already worked against any `cycleId`, not just the current one; the restriction only existed in the UI.) (2) Current Cycle tab's record-payment dialog: added a "الدورة الحالية" (Current Cycle) picker link under the amount field — clicking it lists every other month (past or future) this same member hasn't fully paid, letting the organizer redirect the payment they're about to record to a different month without leaving the Current Cycle tab; picking one relabels the link to that month and re-caps the amount to that month's own outstanding. (3) **New backend field:** `CurrentCycleMemberRowDto.PaidInAdvance` — true when a member finished paying the now-current cycle's contribution while an earlier cycle was still current (i.e. they paid ahead via the Monthly Cycles tab or the picker above) — computed by comparing the contribution's last payment date against the previous cycle's due date. The Current Cycle tab's payments table shows a "دافع مسبقاً" chip next to that member's name when true. Covered by a new test, `RecordingAFutureCyclesContribution_ThenAdvancingToIt_ShowsPaidInAdvance`. | `backend/src/Dourak.Application/Circles/Queries/CircleQueries.cs`; `backend/src/Dourak.Application/Circles/Dtos/CircleDtos.cs`; `backend/tests/Dourak.Application.Tests/CircleWorkflowTests.cs`; `frontend/src/pages/CircleOverview/ScheduleTab.tsx`; `frontend/src/pages/CircleOverview/CurrentCycleTab.tsx`; `frontend/src/pages/CircleOverview/CircleOverviewPage.tsx`; `frontend/src/api/types.ts`; `frontend/src/i18n/en.json` / `ar.json` (`circle.paidInAdvance`) | **Not ported — real feature gap.** The backend change is additive (existing mobile calls to `GET /dashboard` keep working, just gain an unused field), but mobile's Monthly Cycles/Schedule screen has no record-payment button per month at all, and its Current Cycle payment dialog has no cross-month picker or "paid in advance" indicator. |

| 2026-09-20 | Three small frontend-only wording fixes: (1) Current Cycle tab's record-payment dialog now has a "تسجيل المبلغ على:" label before the "الدورة الحالية" month-picker link, instead of the link floating with no explanation. (2) Monthly Cycles tab's "تسجيل دفعة" button now reads "تسجيل دفعة قديمة" for a past month and "تسجيل دفعة مقدما" for a future one — only the current month keeps the plain "تسجيل دفعة" wording. (3) **Real typo fix:** the payout-paid line's "لـ{{recipientName}}" used an unnecessary tatweel (ـ) between the Arabic preposition "ل" and the name, which for some names visually produced a stray-looking "لـا" instead of attaching cleanly — removed the tatweel so it's just "ل{{recipientName}}", exactly how that prefix is normally written. | `frontend/src/pages/CircleOverview/CurrentCycleTab.tsx`; `frontend/src/pages/CircleOverview/ScheduleTab.tsx`; `frontend/src/i18n/en.json` / `ar.json` | Not ported — wording-only, on top of the record-payment/picker feature that isn't ported yet either (see prior 2026-09-20 entry). |

| 2026-09-20 | **New feature + real behavior change (backend + frontend), large.** (1) **Current-cycle selection is now date-driven, not payout-driven:** previously the "current cycle" (Current Cycle tab, dashboard) was whichever cycle still had `Status == Pending`, which advanced the instant a payout was confirmed. Now it's purely calendar-based: the latest cycle whose due date has already arrived, defaulting to the first cycle before anything is due, and staying on the last cycle forever after its own due date (`GetCurrentCycleDashboardQueryHandler`). Confirming a payout no longer "finishes" the current cycle by itself. (2) **Payouts are now paid in installments, like a member's own contribution:** new `PayoutPayment` entity/table (migration `AddPayoutPayments`), `Payout.RecordPayment` is additive (cannot exceed `ExpectedAmount - ActualAmount`) instead of the old all-or-nothing `MarkPaid`; a cycle only moves to Completed once the payout is *fully* paid. `RecordPayoutCommand`/`POST /cycles/{id}/payout` is now multipart and accepts an evidence file (image/PDF, same validation as a payment claim), downloadable via `GET /cycles/payout-payments/{id}/evidence` (organizer or the recipient only). (3) `ConfirmPayoutButton` (Current Cycle tab) now stays visible and re-openable until the payout is fully paid, defaults/caps the amount at what's still outstanding, and has an evidence-attach control. (4) Monthly Cycles tab: the payout line under each month now lists every installment ("تم دفع مبلغ … لـX بتاريخ …" for the first, "{amount} بتاريخ {date}" for the rest, comma-separated), each with a "المرفق" link when it has evidence; a "تسجيل دفعة"-style button now also exists for the recipient — "تأكيد استلام صاحب الدور" (current month), "…لدورة قديمة" (a past month still owed), or "…لدفعة مقدما" (a future month, paid ahead) — for any month whose payout isn't fully paid yet, working the same way the member-contribution "تسجيل دفعة" button already does. Existing tests updated for the new rules (`FullJourney_CreateToPayout_WorksAsExpected` now pays the full pool before asserting completion and asserts the current cycle does *not* advance on payout; new `MovePayoutPosition`/`RecordingAFutureCyclesContribution`/`Activate_WithAPendingInvitee` tests from earlier today remain green); verified end-to-end via live API calls (partial payout → still Pending/current unaffected → remaining installment → Completed → overpay correctly rejected). | `backend/src/Dourak.Domain/Entities/Payout.cs`; `backend/src/Dourak.Domain/Entities/PayoutPayment.cs` (new); `backend/src/Dourak.Application/Circles/Commands/ContributionAndPayoutCommands.cs`; `backend/src/Dourak.Application/Circles/Queries/CircleQueries.cs`; `backend/src/Dourak.Application/Circles/Queries/PayoutPaymentEvidenceQuery.cs` (new); `backend/src/Dourak.Application/Circles/Dtos/CircleDtos.cs`; `backend/src/Dourak.Api/Controllers/CyclesController.cs`; `backend/src/Dourak.Infrastructure/Persistence/DourakDbContext.cs`; `backend/src/Dourak.Infrastructure/Persistence/Migrations/20260920143542_AddPayoutPayments.cs` (new); `backend/tests/Dourak.Application.Tests/CircleWorkflowTests.cs`; `frontend/src/pages/CircleOverview/ConfirmPayoutButton.tsx`; `frontend/src/pages/CircleOverview/ScheduleTab.tsx`; `frontend/src/api/circles.ts`; `frontend/src/api/types.ts`; `frontend/src/i18n/en.json` / `ar.json` | **Not ported — real feature/behavior gap.** Mobile hits the same backend, so its current-cycle display will silently start behaving differently (date-driven instead of payout-driven) with no code change needed there, but the `POST /payout` contract changed shape (multipart instead of JSON) — mobile's existing payout-confirm call will break until updated to multipart, and mobile has no installment history, evidence attach/view, or old/current/advance payout-button variants at all. |

| 2026-09-20 | Three frontend-only tweaks: (1) Current Cycle tab: the "تم الدفع لصاحب الدور" payout badge now shows next to "تحت التحصيل" as soon as the payout is fully paid, even if collection itself isn't finished yet (previously it only ever appeared once collection was done, hiding the fact that the organizer had already paid the recipient in advance from their own pocket). (2) The organizer/member-count line under the circle name now also shows, once there's a current cycle: "القسط: {amount}", "متبقي: {N}" (months left, only shown when this isn't the last month), and "دور: {recipientName}" — appended after "الأعضاء: {count}". (3) The creation-date tooltip trigger now shows just the month and year ("سبتمبر 2026") instead of the full day/month/year; hovering it shows a tooltip with "تاريخ الإنشاء" and the full date spelled out as dd/mm/yyyy. | `frontend/src/pages/CircleOverview/CurrentCycleTab.tsx`; `frontend/src/pages/CircleOverview/CircleOverviewPage.tsx`; `frontend/src/i18n/en.json` / `ar.json` (`circle.installmentLabel`, `circle.remainingMonthsLabel`) | Not ported — cosmetic/informational only, mobile's circle-details header has neither the payout badge nor the extended organizer/members summary line. |

| 2026-09-20 | Two small corrections to the organizer/members header line and its date tooltip added earlier today: (1) removed the "متبقي: {N}" (months remaining) segment entirely — the line now just reads "...الأعضاء: {count}, القسط: {amount}, دور: {recipientName}". (2) The creation-date tooltip's full date swapped month and year: now `dd/yyyy/mm` instead of `dd/mm/yyyy`. | `frontend/src/pages/CircleOverview/CircleOverviewPage.tsx`; `frontend/src/i18n/en.json` / `ar.json` | Not ported — same as the header-line feature these correct (see prior 2026-09-20 entry). |

| 2026-09-20 | Three changes: (1) **Real bug fix** — the creation-date tooltip's day/month/year sequence (added earlier today) was visually reordering to month/year/day because the plain numeric string wasn't isolated from the surrounding Arabic tooltip text's bidi context; fixed the same way as prior bidi bugs in this app, by wrapping it in a `unicodeBidi: isolate` + `direction: ltr` span, and it now reads plainly `dd/mm/yyyy` in the correct day-then-month-then-year order (the earlier "swap month and year" request turned out to be working around this same underlying bug, not an intentional format). (2) The organizer/members header line under the circle name now uses periods instead of commas after the member count and after the installment amount. (3) **New feature:** the Current Cycle tab now shows any payout installments already paid to the current recipient — right under the month timeline — in the exact same format as the Monthly Cycles tab's payout line (first payment spelled out, extra ones as "amount on date", each with a "المرفق" link when it has evidence). New `CurrentCycleDashboardDto.PayoutRows` field (backend) sourced from the same `Payout.Payments` collection the Monthly Cycles tab already reads. Extracted the shared `shortDate` formatter into `frontend/src/utils/date.ts` so both tabs use the identical yy/mm/dd formatting instead of two copies. | `backend/src/Dourak.Application/Circles/Dtos/CircleDtos.cs`; `backend/src/Dourak.Application/Circles/Queries/CircleQueries.cs`; `frontend/src/pages/CircleOverview/CircleOverviewPage.tsx`; `frontend/src/pages/CircleOverview/CurrentCycleTab.tsx`; `frontend/src/pages/CircleOverview/ScheduleTab.tsx`; `frontend/src/utils/date.ts`; `frontend/src/api/types.ts` | **Not ported.** The backend DTO addition is additive (mobile's existing dashboard call keeps working), but mobile's Current Cycle screen has no payout-installment breakdown at all yet, and the header-line/tooltip wording fixes are web-only cosmetics. |

| 2026-09-20 | **Real bug fix + styling tweak**, on top of the tab-shape correction above: the previous edit removed all spacing between tabs (they touched with zero gap) and, separately, the selected tab's text was invisible — `color: "primary.contrastText !important"` doesn't work as an sx value because the whole string (including `!important`) gets treated as one literal token instead of resolving `primary.contrastText` through the theme first, so the browser silently ignored the invalid color and the text fell back to the same shade as its own background. Fixed by adding a small gap between tabs (`.MuiTabs-flexContainer { gap: 0.5 }`) and targeting `.MuiTab-root.Mui-selected` with a proper theme-resolved color (a callback, no `!important` needed) instead of the broken string. | `frontend/src/pages/CircleOverview/CircleOverviewPage.tsx` | Not ported — same as the tab styling above, cosmetic/web-only. |
| 2026-09-20 | Frontend-only styling, twice today: the circle-details page's tabs ("الدورة الحالية"/"الدورات الشهرية"/"الأعضاء"/"السجل") first got a fully pill-shaped tinted background to read more prominently than plain underlined text — but the rounded-all-around shape and the gaps between them ended up looking like separate badges rather than tabs. Corrected to a more traditional tab shape: top corners only rounded, sitting flush against each other on the bottom border (no gaps), still with the light tint / solid-primary-when-selected treatment. | `frontend/src/pages/CircleOverview/CircleOverviewPage.tsx` | Not ported — purely cosmetic, mobile's tab bar uses its own native styling. |

| 2026-09-20 | **New feature (frontend-only):** the active-circle summary card on the home page (`CurrentCycleSummaryCard`) now matches the circle-details page much more closely: (1) the organizer/members line changed to "المنظم: {name}. الأعضاء: {count}. القسط: {amount}." (periods, not commas/middot). (2) The top-right corner, which used to show the current cycle's month, now shows the circle's creation date (month + year) with a tooltip — "تاريخ الإنشاء" plus the full `dd/mm/yyyy`, isolated from bidi reordering the same way the circle-details page's tooltip is. (3) The same month-by-month timeline dots from the circle-details page now render on this card too (`CircleTimeline`, reusing the `["schedule", circleId]` query). (4) Under the timeline, a "الدورة الحالية" label now introduces the badge row, which gained the same "تحت التحصيل"/"تم التحصيل" and "بانتظار الدفع لصاحب الدور"/"تم الدفع لصاحب الدور" badges shown in the Current Cycle tab, next to the existing paid-count badge. (5) The general "جمعياتي" grid card (`CircleInfoCard`) also gained those same two badges next to its status chip, for Active circles only (a new conditional `dashboard` fetch, since that card doesn't otherwise need cycle data). | `frontend/src/pages/CircleOverview/CurrentCycleSummaryCard.tsx`; `frontend/src/pages/Dashboard/DashboardPage.tsx`; `frontend/src/pages/Dashboard/CircleInfoCard.tsx` | Not ported. Mobile's home-screen circle cards have none of the timeline, the extended organizer/members/installment line, the creation-date tooltip, or the collection/payout badges yet. |

| 2026-09-20 | Two frontend-only tweaks: (1) The active-circle card's "الدورة الحالية" label is now bold and a size larger, with "الشهر: {n}/{total}" (current cycle's sequence number over the circle's total month count) shown right next to it. (2) The app's main content container (`AppLayout`) widened from `maxWidth="md"` to `maxWidth="xl"` — the middle content area was cramped with large empty side margins on wider screens. | `frontend/src/pages/CircleOverview/CurrentCycleSummaryCard.tsx`; `frontend/src/layouts/AppLayout.tsx` | Not ported — mobile has no equivalent fixed-width container concept (it's already full-width by nature), and the "الدورة الحالية" card doesn't exist on mobile yet either. |

| 2026-09-20 | Two frontend-only changes: (1) Home page's active-circle card: more top margin above "الدورة الحالية" (now `h6`/bold instead of `subtitle2`), the "صاحب الدور: {name}" line moved from the bottom of the card to sit directly under "الدورة الحالية", and the month detail changed from "الشهر: {n}/{total}" to "الشهر: {monthName} ({ordinal} من {total})" e.g. "الشهر: أكتوبر (الأول من 4)" — extracted the ordinal-word table (previously duplicated inline in `CurrentCycleTab.tsx`) into a shared `frontend/src/utils/monthOrdinal.ts`. (2) Login page: added the same language dropdown (Arabic/English) used in the app header, and a show/hide toggle (eye icon) on the password field. | `frontend/src/pages/CircleOverview/CurrentCycleSummaryCard.tsx`; `frontend/src/pages/CircleOverview/CurrentCycleTab.tsx`; `frontend/src/pages/Login/LoginPage.tsx`; `frontend/src/utils/monthOrdinal.ts` (new); `frontend/src/i18n/en.json` / `ar.json` (`circle.monthOrdinalDetail`) | Not ported. Mobile's login screen has no language selector or password-visibility toggle, and its home-screen circle card (if any) doesn't have this month/recipient layout. |

| 2026-09-20 | Two frontend-only tweaks: (1) Login page's language dropdown (added earlier today) moved from its own row above the logo down to the bottom row, next to "ليس لديك حساب؟ إنشاء حساب" — a plain `justifyContent: "space-between"` row, which in Arabic naturally puts the first child (the sign-up text) on the right and the second (the dropdown) on the far left, no direction hack needed. (2) Home page's active-circle card: "الدورة الحالية" shrank from `h6` to `subtitle2`, and the "الشهر: " label word next to it was dropped — it now just reads "{monthName} ({ordinal} من {total})" directly. | `frontend/src/pages/Login/LoginPage.tsx`; `frontend/src/pages/CircleOverview/CurrentCycleSummaryCard.tsx` | Not ported — same as the features these adjust (see earlier 2026-09-20 entries). |

| 2026-09-20 | Home page's active-circle card: the "القسط: {amount}" segment now includes the currency code, e.g. "القسط: 100 SAR". | `frontend/src/pages/CircleOverview/CurrentCycleSummaryCard.tsx` | Not ported — same as the header-line feature it adjusts (see earlier 2026-09-20 entries). |

| 2026-09-20 | **Real bug fix + new feature (frontend-only):** (1) Fixed a real bug — submitting a new payment claim ("سجل دفعتي") never invalidated the `["myPaymentClaims"]` query, so the member's own "دفعة معلقة" badge stayed non-clickable (no claim data to open a dialog with) until something else happened to refetch it — a manual page refresh being the only reliable trigger. Added the missing invalidation, plus `["monthsDetail", circleId]` (was also missing) to both the submit and the edit/withdraw (`MyClaimDialog`) mutations. (2) **New feature:** a member submitting their own payment claim now gets the same "الدورة الحالية" month-picker the organizer already had for recording a payment directly — letting them redirect the claim to any other month (past, still unpaid, or a future one not yet fully paid) instead of only ever the current cycle; the claimed amount is capped at that month's own outstanding balance, with the same red-border/error-message feedback pattern used elsewhere. `CurrentCycleTab` now fetches `monthsDetail` for every viewer (not just the organizer) since the underlying amounts were already visible to everyone — only claim status is privacy-masked. | `frontend/src/pages/CircleOverview/PaymentClaimDialogs.tsx` (`SubmitPaymentClaimDialog`, `MyClaimDialog`); `frontend/src/pages/CircleOverview/CurrentCycleTab.tsx` | Not ported. Mobile's own claim-submission flow (if any) is locked to the current cycle only, and likely has the same stale-badge-after-submit bug since it probably shares the same "claims I submitted" caching pattern. |

| 2026-09-20 | Two frontend-only fixes for a member's own claim flow: (1) "سجل دفعتي" (Current Cycle tab) no longer disappears just because the *current* cycle happens to be fully paid — it now also shows whenever any other month (past or future) still has an outstanding balance, and the dialog defaults straight to the first such month (instead of opening on a dead-end "0 outstanding" current-cycle target) when that's the only place left to claim. (2) **Real bug fix:** clicking a member's own "دفعة معلقة" badge in the Monthly Cycles tab always opened the read-only detail dialog, even for their own still-Pending claim — there was no way to edit or withdraw it from that tab (only from the Current Cycle tab). Now it opens the same editable `MyClaimDialog` there too, exactly like the Current Cycle tab already does, when the claim is both theirs and still Pending. | `frontend/src/pages/CircleOverview/PaymentClaimDialogs.tsx` (`SubmitPaymentClaimDialog`); `frontend/src/pages/CircleOverview/CurrentCycleTab.tsx`; `frontend/src/pages/CircleOverview/ScheduleTab.tsx`; `frontend/src/pages/CircleOverview/CircleOverviewPage.tsx` | Not ported — mobile's claim flow (if any) likely has the same "only editable from one screen" gap and the same "hidden once current cycle is paid" limitation. |

## Longer-standing known gaps (pre-dating this log)

- Mobile has no equivalent screens/flows for: forgot-password / reset-password, email verification banner + resend. See `docs/future-work.md`.
